// src/app/page.tsx
// Placeholder temporal — aquí irá la landing page de PsyDesk
// Por ahora redirige al login si no hay sesión (el middleware lo maneja)
// y al dashboard si hay sesión activa.

export default function RootPage() {
  return null;
}